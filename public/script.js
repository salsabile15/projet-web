const { router } = require("../app")

function getArticles(take = 10 , skip = 0) : Promise
fch ndiru promise fih get 
xhr.open = get 
lien = chemin (ca depend node js) Public./route/articles.js 
function getArticles(id?)
function postArticles()
function patchArticles()
function deleteArticles()
//meme pour user?categories?comment?

//Toutes les fonctions devraient retourner une Promesse qui, lorsqu'elle sera résolue, retournera les données attendues.
function getArticles(take = 10 , skip = 0){
    if(router.get()){
            console.log('success')
            
    }
    else {
        console.log('error')
    }
}